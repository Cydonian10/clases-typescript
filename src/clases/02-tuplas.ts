const prices: Array<number | string> = [ 1, 2, 3, 4, 5, 'hola' ];

const user: [ string, number ] = [ "nicobytes", 15 ];
/**
 * !Desestructura a User ["nicobytes",15],
 * !                     [username,age]
 */
const [ username, age ] = user;

